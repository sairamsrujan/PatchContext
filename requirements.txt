# Pinned on Python 3.11 (Phase 0). Reproduce with:
#   uv venv --python 3.11 .venv && uv pip install -r requirements.txt -e .
httpx==0.28.1
pydantic-settings==2.14.2
langchain==1.3.13
langchain-community==0.4.2
sentence-transformers==5.6.0
faiss-cpu==1.14.3
pandas==3.0.3
pyarrow==25.0.0
openai==2.45.0
tenacity==9.1.4
streamlit==1.59.1
ragas==0.4.3
pytest==9.1.1

# Key transitive pins (loaded in-process; pinned for reproducibility)
torch==2.13.0
transformers==5.13.1
numpy==2.4.6
