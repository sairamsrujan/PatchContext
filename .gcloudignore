# Excluded from the source upload for `gcloud run deploy --source .`
# (Cloud Build only needs what the Dockerfile COPYs; the venv/git history
# would otherwise add >1 GB to every deploy upload).
.git
.venv
.env
__pycache__
*.pyc
*.egg-info
.pytest_cache
data/raw
.DS_Store
data/index/faiss.index
